// 简单的编译测试
让 x = 10
让 y = 20
打印(x + y)
打印("Hello, World!")

// 测试数组
让 数组 = [1, 2, 3, 4, 5]
打印("数组长度: " + 数组.长度())
打印("第一个元素: " + 数组[0])

// 测试条件语句
如果 x > 5 {
    打印("x 大于 5")
} 否则 {
    打印("x 小于等于 5")
}

// 测试循环
让 i = 0
循环 i < 3 {
    打印("循环次数: " + i)
    i = i + 1
}