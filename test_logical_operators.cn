// 测试逻辑运算符

// 测试且运算符 (&&)
让 a = 真
让 b = 假
打印("a 且 b:")
打印(a 且 b)

// 测试或运算符 (||)
打印("a 或 b:")
打印(a 或 b)

// 测试非运算符 (!)
打印("非 a:")
打印(非 a)
打印("非 b:")
打印(非 b)

// 测试复杂逻辑表达式
让 x = 5
让 y = 10
打印("x > 3 且 y < 15:")
打印(x > 3 且 y < 15)

打印("x > 10 或 y < 15:")
打印(x > 10 或 y < 15)

打印("非 (x > 3):")
打印(非 (x > 3))

// 测试布尔值与数字的逻辑运算
打印("1 且 0:")
打印(1 且 0)

打印("1 或 0:")
打印(1 或 0)

打印("非 0:")
打印(非 0)

// 测试字符串的逻辑运算
让 s1 = "hello"
让 s2 = ""
打印("s1 且 s2:")
打印(s1 且 s2)

打印("s1 或 s2:")
打印(s1 或 s2)

打印("非 s2:")
打印(非 s2)

打印("逻辑运算符测试完成！")